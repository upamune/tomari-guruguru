# トマリぐるぐる / トマリトーク

マウスに追従して25方向に振り向き、音声に合わせて口パク・まばたきする、トマリ用のブラウザアバターです。

- **トマリぐるぐる**: マウス追従でキャラクターがこっちを見るシンプル版
- **トマリトーク**: マイク入力または音声ファイルに合わせて口パクするトーク版

---

## まず使う場合

Windowsなら `start.bat` をダブルクリックすると、ローカルサーバーを起動してブラウザで開きます。

手動で起動する場合は、このフォルダで以下を実行します。

```bash
python -m http.server 8000
```

その後、ブラウザで開きます。

```text
http://localhost:8000/トマリトーク.html
```

または:

```text
http://localhost:8000/トマリぐるぐる.html
```

注意:

- HTMLを直接ダブルクリックする `file://` では、JSX読み込みがブラウザ制限で動かない場合があります。
- マイク入力は `localhost` または HTTPS でのみ利用できます。
- React / Babel / Google Fonts はCDNから読み込むため、初回表示にはネット接続が必要です。

---

## 現在入っているもの

### アプリ本体

| ファイル / フォルダ | 役割 |
|---|---|
| `トマリぐるぐる.html` | ぐるぐる版の起動ページ |
| `トマリトーク.html` | トーク版の起動ページ |
| `app.jsx` | ぐるぐる版のアプリ本体 |
| `talk-app.jsx` | トーク版のアプリ本体 |
| `tweaks-panel.jsx` | 画面右下の調整パネル |
| `slices2/A〜F/` | 実際に表示するスライス済みキャラ画像。6状態 × 25方向 = 150枚の WebP |
| `start.bat` | Windowsでローカルサーバーを起動する補助ファイル |
| `LICENSE` | プログラム部分に適用するMITライセンス |
| `ASSET_LICENSE.md` | キャラクター画像・音声など、素材部分の利用条件 |
| `.thumbnail` | サムネイル用ファイル |
| `再生用トマリセリフ.wav` | 口パク確認用のサンプル音声。公開に必須ではありません |

### 画像素材を作るときの補助ファイル

| ファイル / フォルダ | 役割 |
|---|---|
| `01_画像指示例.png` | 画像生成時のやり取り・指示例のスクリーンショット |
| `01_画像生成用テンプレ.png` | 5×5顔角度テンプレート画像 |
| `01_画像生成用プロンプト.txt` | 画像生成に使うプロンプト・追加指示文 |
| `新キャラ差し替え手順.md` | 新キャラに差し替えるときの詳しい生成・検証手順 |
| `tools/slice_character_sheets.py` | 5×5シート画像を `slices2/` 用に切り出すPythonスクリプト |

`uploads/` と `sheets/` は公開版には同梱していません。再生成コマンドを実行すると、必要に応じて作成されます。

---

## フレーム画像の仕組み

このアプリは、キャラクターの向きと表情に応じて `slices2/` 内の画像を1枚ずつ切り替えています。

### 25方向

5列 × 5行の向き差分です。

- 列: 左向き → 正面 → 右向き
  - `c0`: 左向き
  - `c1`: 左斜め
  - `c2`: 正面
  - `c3`: 右斜め
  - `c4`: 右向き
- 行: 上向き → 水平 → 下向き
  - `r0`: 強く上を見る
  - `r1`: 少し上を見る
  - `r2`: 水平
  - `r3`: 少し下を見る
  - `r4`: 強く下を見る

### 6状態

| フォルダ | 目 | 口 |
|---|---|---|
| `A` | 開け | とじ |
| `B` | 開け | 中間 |
| `C` | 開け | 開け |
| `D` | 閉じ | とじ |
| `E` | 閉じ | 中間 |
| `F` | 閉じ | 開け |

表示画像のパス例:

```text
slices2/A/r2c2.webp
slices2/D/r2c2.webp
slices2/F/r4c4.webp
```

現在のトマリ版は **WebP形式** を参照しています。

---

## 使い方

### トマリぐるぐる

1. `トマリぐるぐる.html` を開く
2. マウスを動かす
3. キャラクターがマウス方向に合わせて25方向で振り向きます
4. 自動まばたきも入ります

### トマリトーク

1. `トマリトーク.html` を開く
2. **マイク開始** を押す、または音声ファイルを読み込む
3. 音量に応じて口が切り替わります
   - とじ
   - はんびらき
   - ぜんかい
4. まばたき時は `D/E/F` の目閉じ画像に切り替わります

---

## Tweaks 調整パネル

画面右下の **Tweaks** ボタンから調整できます。

主な項目:

- マイク感度
- 口パクのしきい値
- 口を閉じる速さ
- 自動まばたき
- 追従範囲
- 追従速度
- キャラサイズ
- 背景色

OBSなどで使う場合は、背景色をクロマキーしやすい色に調整してください。

---

## GitHub Pagesなどで公開する場合

静的ファイルだけなので、GitHub Pages / Netlify / Cloudflare Pages などにそのまま置けば動きます。

### アプリ公開に最低限必要なもの

```text
トマリぐるぐる.html
トマリトーク.html
app.jsx
talk-app.jsx
tweaks-panel.jsx
slices2/
```

### あると便利なもの

```text
README.md
LICENSE
ASSET_LICENSE.md
start.bat
再生用トマリセリフ.wav
```

`再生用トマリセリフ.wav` は口パク確認用のサンプルです。不要なら公開から外してもアプリは動きます。

### 他の人が素材差し替えできるようにする場合に含めるもの

```text
01_画像指示例.png
01_画像生成用テンプレ.png
01_画像生成用プロンプト.txt
新キャラ差し替え手順.md
tools/slice_character_sheets.py
```

---

## 他の人が自分のキャラで作るには

キャラクター参照画像を1枚用意して、画像生成AIに「このキャラで差し替えて」と頼むだけでも入口にはなります。

ただし、このアプリで動かすには最終的に **5×5角度シートを6枚** 作る必要があります。

必要な6枚:

```text
A_目開け_口とじ.png
B_目開け_口中間.png
C_目開け_口開け.png
D_目閉じ_口とじ.png
E_目閉じ_口中間.png
F_目閉じ_口開け.png
```

おすすめの流れ:

1. 自分のキャラクター参照画像を用意する
2. `01_画像生成用テンプレ.png` を角度テンプレートとして添付する
3. `01_画像生成用プロンプト.txt` の最初の指示を使って、`A_目開け_口とじ.png` 相当の5×5シートを作る
4. 同じ画像に対して追加指示を出し、目閉じ版・口中間版・口開け版を作る
5. 6枚のPNGを `新キャラ資料/` フォルダに入れる
6. `tools/slice_character_sheets.py` で `slices2/` に切り出す
7. ローカルで表示確認する

詳しい注意点や検証方法は `新キャラ差し替え手順.md` を参照してください。

---

## スライス画像を再生成する例

新しい6枚の元画像を `新キャラ資料/` に入れた場合の例です。

```powershell
python tools\slice_character_sheets.py `
  --source "新キャラ資料" `
  --sheets-out "sheets" `
  --uploads-out "uploads" `
  --slices-out "slices2" `
  --format webp `
  --jobs 4 `
  --component-mode `
  --min-component-area 80 `
  --alpha-threshold 64 `
  --remove-gray-residue
```

生成後、以下のようなファイルが150枚できます。

```text
slices2/A/r0c0.webp ... r4c4.webp
slices2/B/r0c0.webp ... r4c4.webp
...
slices2/F/r0c0.webp ... r4c4.webp
```

---

## ライセンス

このリポジトリは、**プログラム部分** と **キャラクター素材・音声** でライセンスを分けています。

### プログラム部分

プログラムコードは MIT License で公開しています。詳細は `LICENSE` を参照してください。

MIT License の対象例:

```text
*.html
*.jsx
*.bat
tools/*.py
```

### キャラクター画像・音声・生成素材

キャラクター画像、スライス済みフレーム、キャラクターを含むスクリーンショット、サムネイル、音声ファイルは MIT License の対象外です。

通常の動作確認、紹介、感想、バグ報告などを目的として、**非商用の範囲でSNSやブログにスクリーンショットを投稿することはOK**です。アプリが動いている様子を短い画面録画として投稿することも、非商用の範囲であればOKです。

一方で、トマリのキャラクター画像や音声を、**自分のキャラクター、自分のアバター、自分の素材として使うこと、商用利用すること、他プロジェクトへ流用することは禁止**です。詳細は `ASSET_LICENSE.md` を参照してください。

素材ライセンスの対象例:

```text
slices2/**
.thumbnail
01_画像指示例.png
再生用トマリセリフ.wav
```

### 自分のキャラへ差し替える場合

このアプリのコードや手順を使って、自分が権利を持つキャラクター画像に差し替えることは可能です。

その場合、自分で用意した差し替え素材の権利は、その素材の権利者に従います。トマリのキャラクター画像や音声を流用しないでください。

---

## 公開前の注意点

- 現在のアプリは `.webp` を参照しています。PNGで出力した場合は `app.jsx` と `talk-app.jsx` の拡張子も `.png` に変更してください。
- `slices2/` を削除するとキャラクターが表示されません。
- `uploads/` と `sheets/` は公開版には同梱していません。元画像を残したい場合はローカルで別途バックアップしてください。
- GitHubに公開する場合、`LICENSE` と `ASSET_LICENSE.md` を含めて、コードと素材の利用条件が分かるようにしてください。
- キャラクター画像・音声・生成素材の権利や利用条件を確認してください。

